name = "compliance-iegm-dashboard"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# D1 binding will be managed manually in the Cloudflare Pages dashboard
# to avoid conflicts with the automatic wrangler.toml detection.